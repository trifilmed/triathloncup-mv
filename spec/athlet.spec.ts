// import { Athlet, KonkreterAthlet } from '../src/athlet';

describe('KonkreterAthlet', () => {
    it('wird korrekt instanziert.', () => {
    //     let erwarteterVorname: string = 'Dennis';
    //     let erwarteterNachname: string = 'Weu';
    //     let erwarteteAltersklasse: string = 'M25';

    //     let athlet: Athlet = new KonkreterAthlet(erwarteterNachname, erwarteterVorname, erwarteterVorname);

    //     expect(athlet.getName()).toBe(erwarteterNachname);
    //     expect(athlet.getVorname()).toBe(erwarteterNachname);
    //     expect(athlet.getAltersklasse()).toBe(erwarteteAltersklasse);
    fail();
    });
});